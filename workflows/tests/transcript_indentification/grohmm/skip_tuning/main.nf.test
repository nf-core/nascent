nextflow_pipeline {

    name "groHMM"
    script "../../../../../main.nf"
    tag "groHMM"

    test("Should be able to skip tuning") {

        when {
            params {
                outdir = "$outputDir"
                skip_grohmm = false
                skip_tuning = true
            }
        }

        then {
            assertAll(
                // TODO Support a tuning file
                { assert workflow.failed },
            )
        }
    }
}
